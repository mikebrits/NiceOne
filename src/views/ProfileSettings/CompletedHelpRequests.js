/**
 *
 * @flow
 */

import React, { Component } from 'react';
import {
    StyleSheet,
    View
} from 'react-native';

class CompletedHelpRequests extends Component {
    render() {
        return (
            <View> </View>
        );
    }
}

export default CompletedHelpRequests;

const styles = StyleSheet.create({
});
