/**
 *
 * @flow
 */

import React, { Component } from 'react';
import {
    StyleSheet,
    View,
    Text
} from 'react-native';

class MakeOffer extends Component {
    render() {
        return (
            <View>
                <Text> Make Offer Request</Text>
            </View>
        );
    }
}

export default MakeOffer;

const styles = StyleSheet.create({
});
