/**
 *
 * @flow
 */

import React, { Component } from 'react';
import {
    StyleSheet,
    View
} from 'react-native';

class Chat extends Component {
    render() {
        return (
            <View> </View>
        );
    }
}

export default Chat;

const styles = StyleSheet.create({
});
